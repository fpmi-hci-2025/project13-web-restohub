<div class="container py-4 text-white addresses-page">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="mb-0"><%= t('addresses.edit_title') %></h2>

    <%= link_to t('addresses.back_to_index'),
                addresses_path(locale: I18n.locale),
                class: "btn btn-outline-secondary btn-sm" %>
  </div>

  <div class="card bg-dark border-0 rounded-4 p-4">
    <%= render "form", address: @address %>
  </div>
</div>